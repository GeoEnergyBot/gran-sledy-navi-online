(()=>{
  const core=window.GranCore;if(!core)return;
  const $=s=>document.querySelector(s);
  const state={session:null,stream:null,answers:[],sequence:[],errors:0,focusMs:0,focusBreaks:0,observations:0,raf:null,last:0,mode:null,demo:true};
  const dist=(a,b)=>{const R=6371000,p1=a.lat*Math.PI/180,p2=b.lat*Math.PI/180,dp=(b.lat-a.lat)*Math.PI/180,dl=(b.lng-a.lng)*Math.PI/180;return 2*R*Math.asin(Math.sqrt(Math.sin(dp/2)**2+Math.cos(p1)*Math.cos(p2)*Math.sin(dl/2)**2))};
  function selected(){return core.state.world.find(x=>x.id===core.state.selectedEventId)||null}
  function stopCamera(){if(state.stream){state.stream.getTracks().forEach(t=>t.stop());state.stream=null}cancelAnimationFrame(state.raf);state.raf=null}
  function closeAR(reload=false){stopCamera();$('#spiritAr')?.remove();state.session=null;state.mode=null;if(reload)setTimeout(()=>location.reload(),250)}
  async function startCamera(video){try{state.stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:{ideal:'environment'}},audio:false});video.srcObject=state.stream;await video.play()}catch{video.classList.add('hidden');$('#spiritFallback')?.classList.remove('hidden')}}
  function shell(session){const root=document.createElement('section');root.id='spiritAr';root.className='spirit-ar';root.innerHTML=`<video id="spiritVideo" class="spirit-camera" autoplay muted playsinline></video><div id="spiritFallback" class="spirit-camera-fallback hidden"></div><div class="spirit-fog"></div><div class="spirit-particles"></div><div class="spirit-ar-head"><button id="spiritClose">×</button><div class="spirit-ar-title"><b>${session.creature.name}</b><small>AR-встреча · ${session.event.title}</small></div><button id="spiritCameraToggle">◉</button></div><div class="spirit-stage"><div class="spirit-reticle"></div><img id="spiritCreature" class="spirit-creature revealing" src="${session.creature.image}" alt="${session.creature.name}"></div><div id="spiritPanel" class="spirit-panel"><h3>Проявление существа</h3><p>Медленно осмотрите пространство. Домовой выходит из Нави…</p><div class="spirit-progress"><span style="width:12%"></span></div></div>`;document.body.append(root);$('#spiritClose').onclick=()=>closeAR();$('#spiritCameraToggle').onclick=()=>{const video=$('#spiritVideo');video.classList.toggle('hidden');$('#spiritFallback').classList.toggle('hidden',!video.classList.contains('hidden'))};startCamera($('#spiritVideo'));setTimeout(showChoices,2400)}
  function showChoices(){const p=$('#spiritPanel');if(!p)return;p.innerHTML=`<h3>Как поступить?</h3><p>Домовой насторожен. Выберите способ взаимодействия.</p><div class="spirit-actions"><button class="spirit-action calm" data-mode="calm"><b>♡</b>Договориться</button><button class="spirit-action study" data-mode="study"><b>⌖</b>Исследовать</button><button class="spirit-action banish" data-mode="banish"><b>ᛉ</b>Изгнать</button></div>`;p.querySelectorAll('[data-mode]').forEach(b=>b.onclick=()=>openMode(b.dataset.mode))}
  function openMode(mode){state.mode=mode;if(mode==='study')studyMode();else if(mode==='calm')calmMode(0);else banishMode()}
  function studyMode(){state.focusMs=0;state.focusBreaks=0;state.observations=0;state.last=performance.now();const p=$('#spiritPanel');p.innerHTML=`<h3>Наблюдение</h3><p>Удерживайте Домового внутри круга. Плавно поворачивайте телефон.</p><div class="spirit-progress"><span id="studyBar"></span></div><div class="spirit-metrics"><span id="studyTime">0.0 / 12 сек.</span><span id="studyBreaks">Потери фокуса: 0/3</span></div><button id="studyObserve" class="spirit-secondary">Зафиксировать деталь</button><button id="studyStabilize" class="spirit-primary">Стабилизировать фокус</button>`;$('#studyObserve').onclick=()=>{state.observations++;$('#studyObserve').textContent=`Наблюдений: ${state.observations}`};let stabilizedUntil=0;$('#studyStabilize').onpointerdown=()=>stabilizedUntil=performance.now()+1500;const creature=$('#spiritCreature');let angleX=0,angleY=0;const orient=e=>{angleX=Math.max(-35,Math.min(35,e.gamma||0));angleY=Math.max(-25,Math.min(25,e.beta?e.beta-45:0))};window.addEventListener('deviceorientation',orient,{passive:true,once:false});const tick=t=>{const dt=Math.min(60,t-state.last);state.last=t;const driftX=Math.sin(t/1300)*65+angleX*2,driftY=Math.cos(t/1700)*42+angleY*1.5;creature.style.transform=`translate(${driftX}px,${driftY}px)`;const focused=Math.hypot(driftX,driftY)<95||t<stabilizedUntil;if(focused)state.focusMs+=dt;else if(Math.floor(t/900)!==Math.floor((t-dt)/900))state.focusBreaks++;$('#studyBar').style.width=`${Math.min(100,state.focusMs/120)}%`;$('#studyTime').textContent=`${(state.focusMs/1000).toFixed(1)} / 12 сек.`;$('#studyBreaks').textContent=`Потери фокуса: ${state.focusBreaks}/3`;if(state.focusMs>=12000||state.focusBreaks>3){window.removeEventListener('deviceorientation',orient);finish('study',{focusMs:state.focusMs,focusBreaks:state.focusBreaks,observations:state.observations});return}state.raf=requestAnimationFrame(tick)};state.raf=requestAnimationFrame(tick)}
  function calmMode(round){const cfg=state.session.interaction.calm.dialogue[round],p=$('#spiritPanel');if(!cfg){finish('calm',{answers:state.answers});return}p.innerHTML=`<h3>Договор с Домовым</h3><div class="spirit-dialogue">${cfg.line}</div><div class="spirit-options">${cfg.options.map(o=>`<button data-answer="${o.id}">${o.text}</button>`).join('')}</div><div class="spirit-metrics"><span>Шаг ${round+1}/3</span><span>Доверие формируется</span></div>`;p.querySelectorAll('[data-answer]').forEach(b=>b.onclick=()=>{state.answers.push(b.dataset.answer);calmMode(round+1)})}
  function banishMode(){state.sequence=[];state.errors=0;const known=new Set(state.session.interaction.knownRunes.map(r=>r.id)),runes=state.session.interaction.runePool,p=$('#spiritPanel');p.innerHTML=`<h3>Три печати изгнания</h3><p>Выберите руны в правильном порядке. Изученные руны отмечены золотом.</p><div class="spirit-rune-slots">${[0,1,2].map(i=>`<div class="spirit-rune-slot" data-slot="${i}">?</div>`).join('')}</div><div class="spirit-runes">${runes.map(r=>`<button class="spirit-rune ${known.has(r.id)?'known':''}" data-rune="${r.id}" title="${r.name}">${r.symbol}</button>`).join('')}</div><button id="runeReset" class="spirit-secondary">Сбросить</button><button id="runeSubmit" class="spirit-primary" disabled>Замкнуть печати</button>`;p.querySelectorAll('[data-rune]').forEach(b=>b.onclick=()=>{if(state.sequence.length>=3)return;state.sequence.push(b.dataset.rune);const rune=runes.find(r=>r.id===b.dataset.rune);p.querySelector(`[data-slot="${state.sequence.length-1}"]`).textContent=rune.symbol;$('#runeSubmit').disabled=state.sequence.length<3});$('#runeReset').onclick=()=>banishMode();$('#runeSubmit').onclick=()=>finish('banish',{sequence:state.sequence,errors:state.errors})}
  async function finish(mode,payload){const p=$('#spiritPanel');p.innerHTML='<h3>Грань отвечает…</h3><p>Результат взаимодействия закрепляется в мире.</p><div class="spirit-progress"><span style="width:100%"></span></div>';try{const data=await core.post('/api/spirit/resolve',{challengeId:state.session.challengeId,mode,payload});showResult(data)}catch(e){p.innerHTML=`<h3>Встреча сорвалась</h3><p>${e.message}</p><button class="spirit-primary" id="spiritRetry">Закрыть</button>`;$('#spiritRetry').onclick=()=>closeAR()}}
  function showResult(data){const p=$('#spiritPanel');if(!data.success){p.innerHTML=`<div class="spirit-result"><div class="seal">×</div><h2>Домовой исчез</h2><p>Попытка не удалась. Полученные знания сохранятся частично.</p><button class="spirit-primary" id="spiritDone">Вернуться на карту</button></div>`;$('#spiritDone').onclick=()=>closeAR(true);return}const detail=data.detail||{},extra=detail.unlockedRune?`<div>Открыта руна<b>${detail.unlockedRune.symbol} ${detail.unlockedRune.name}</b></div>`:detail.glyph?`<div>Найден глиф<b>${detail.glyph.rune.symbol} ${detail.glyph.rune.name}</b></div>`:'';p.innerHTML=`<div class="spirit-result"><div class="seal">${data.mode==='calm'?'♡':data.mode==='study'?'⌖':'ᛉ'}</div><h2>${data.mode==='calm'?'Договор заключён':data.mode==='study'?'Существо изучено':'Домовой изгнан'}</h2><p>Ваш выбор изменил отношение духа и состояние района.</p><div class="spirit-loot"><div>Опыт<b>+${data.reward?.xp||0}</b></div><div>Эссенция<b>+${data.reward?.essence||0}</b></div>${extra}</div><button class="spirit-primary" id="spiritDone">Вернуться на карту</button></div>`;core.state.player=data.player;$('#level').textContent=data.player.level;$('#essence').textContent=data.player.essence;$('#signs').textContent=data.player.signs;$('#spiritDone').onclick=()=>closeAR(true)}
  async function begin(obj){const d=dist(core.state.position,obj);if(d>30&&!state.demo){alert(`Подойдите ближе: осталось ${Math.round(d-30)} м`);return}try{state.session=await core.post('/api/spirit/start',{eventId:obj.id,...core.state.position});state.answers=[];shell(state.session)}catch(e){alert(e.message)}}
  core.on('api:response',({path,data})=>{if(path==='/api/bootstrap')state.demo=Boolean(data.server?.demoMode)});
  document.addEventListener('click',e=>{const btn=e.target.closest('#startEncounter');if(!btn)return;const obj=selected();if(obj?.creatureId!=='domovoy')return;e.preventDefault();e.stopImmediatePropagation();begin(obj)},true);
})();