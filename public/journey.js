(()=>{
  const core=window.GranCore;if(!core)return;
  const J={player:null,world:[],scanned:false};
  const $=s=>document.querySelector(s);
  function esc(s=''){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
  function completedEncounters(){return Number(J.player?.stats?.encounters||0)}
  function journeyStage(){if(completedEncounters()>0)return 3;if(J.scanned&&J.world.length)return 2;if(J.scanned)return 1;return 0}
  function ensureJourney(){const view=$('.view[data-view="map"]');if(!view||$('.journey-card'))return;const card=document.createElement('section');card.className='journey-card';card.innerHTML='<div class="journey-head"><span>Первое дело</span><button type="button" aria-label="Скрыть">×</button></div><h3></h3><p></p><div class="journey-progress"></div><button class="journey-action" type="button"></button>';view.append(card);card.querySelector('.journey-head button').onclick=()=>{card.classList.add('hidden');localStorage.setItem('gran_first_journey_hidden','1')};card.querySelector('.journey-action').onclick=performJourneyAction}
  function renderJourney(){ensureJourney();const card=$('.journey-card');if(!card||localStorage.getItem('gran_first_journey_hidden')==='1')return;const stage=journeyStage();if(stage===3){card.classList.add('hidden');localStorage.setItem('gran_first_journey_done','1');return}const content=[['Первый шёпот','Активируйте Импульс Нави и проявите скрытые следы вокруг себя.','Активировать Импульс'],['Грань раскрывается','Подождите, пока сканер покажет ближайшие события общего мира.','Обновить карту'],['Выберите ближайший след','Нажмите ближайший объект на карте, подойдите к нему и завершите первую встречу.','Показать ближайший след']][stage];card.classList.remove('hidden');card.querySelector('h3').textContent=content[0];card.querySelector('p').textContent=content[1];card.querySelector('.journey-action').textContent=content[2];card.querySelector('.journey-progress').innerHTML=[['ᚾ','Импульс',stage>0],['⌁','Найти',stage>1],['◉','Встреча',false]].map(([icon,label,done])=>`<div class="journey-step ${done?'done':''}"><b>${done?'✓':icon}</b>${label}</div>`).join('')}
  function performJourneyAction(){const stage=journeyStage();if(stage===0){$('#navPulse')?.click();return}if(stage===1){$('#locateBtn')?.click();setTimeout(renderJourney,700);return}const nearest=$('.nearest-objective button');if(nearest)nearest.click();else $('#navPulse')?.click()}
  function enhanceReward(){const root=$('#sheetContent');if(!root||root.dataset.rewardEnhanced==='1'||!root.textContent.includes('Грань откликнулась'))return;const lines=[...root.querySelectorAll('.card p')].map(p=>p.textContent.trim()).filter(Boolean);const get=prefix=>lines.find(x=>x.startsWith(prefix))?.split(':').slice(1).join(':').trim()||'';const xp=get('Опыт'),essence=get('Эссенция'),signs=get('Знаки Грани'),rift=get('Вклад в разлом');const artifacts=lines.filter(x=>x.includes('артефакт'));const items=lines.filter(x=>!x.startsWith('Опыт')&&!x.startsWith('Эссенция')&&!x.startsWith('Знаки Грани')&&!x.startsWith('Вклад в разлом')&&!x.includes('артефакт'));root.dataset.rewardEnhanced='1';root.innerHTML=`<div class="reward-hero"><div class="reward-seal">ᚾ</div><div class="reward-title">Грань откликнулась</div><div class="reward-subtitle">Ваша история стала частью общего мира.</div></div><div class="reward-grid">${xp?`<div class="reward-tile"><span>Опыт</span><b>${esc(xp)}</b></div>`:''}${essence?`<div class="reward-tile"><span>Эссенция</span><b>${esc(essence)}</b></div>`:''}${signs?`<div class="reward-tile"><span>Знаки Грани</span><b>${esc(signs)}</b></div>`:''}${rift?`<div class="reward-tile"><span>Вклад в разлом</span><b>${esc(rift)}</b></div>`:''}</div>${items.map(x=>`<div class="reward-item"><i>✦</i><div>${esc(x)}<small>Добавлено в инвентарь</small></div></div>`).join('')}${artifacts.map(x=>`<div class="reward-item"><i>◇</i><div>${esc(x)}<small>Сохранено в коллекции</small></div></div>`).join('')}<div class="reward-next">Откройте Журнал или отправляйтесь в Убежище.</div><button class="primary" style="width:100%;margin-top:12px" id="rewardContinue">Продолжить путь</button>`;$('#rewardContinue').onclick=()=>$('#sheetClose')?.click()}
  core.on('player:updated',player=>{J.player=player;renderJourney()});
  core.on('world:loaded',data=>{J.world=data.objects||[];renderJourney()});
  core.on('api:response',({path,data,ok})=>{if(path==='/api/encounters/resolve'&&ok){if(data.player)J.player=data.player;setTimeout(()=>{renderJourney();enhanceReward()},80)}});
  window.addEventListener('load',()=>{ensureJourney();const pulse=$('#navPulse');pulse?.addEventListener('click',()=>{J.scanned=true;renderJourney();setTimeout(()=>{J.scanned=false;renderJourney()},65000)});setTimeout(renderJourney,1200)});
})();